import React from 'react';

interface HomeProps {}

const Home: React.FC<HomeProps> = ({}) => {
    return (
        <div>
            <h2>Home Page</h2>
        </div>
    );
};

export default Home;
